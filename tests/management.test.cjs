const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),path=require('node:path');
class El{constructor(tag){this.tag=tag;this.children=[];this.dataset={};this.className='';this.classList={add:()=>{}};this.textContent='';this.onclick=null}append(...items){this.children.push(...items)}replaceChildren(...items){this.children=[...items]}querySelectorAll(){return this.children.filter(x=>x!==this.children[0])}get firstElementChild(){return this.children[0]}click(){this.onclick?.()}set innerHTML(_){throw new Error('unsafe innerHTML')}}
const document={createElement:tag=>new El(tag),createTextNode:value=>({textContent:String(value)})};
const allText=node=>[node?.textContent||'',...(node?.children||[]).flatMap(allText)];
test('Management renders authoritative malicious text without HTML interpretation',async()=>{const root=new El('root'),transport={snapshot:async()=>({version:4,correlationId:'snapshot-1',companies:[{name:'<img src=x onerror=1>'}]}),intent:async()=>({state:'Succeeded'}),exportLogs(){}};const context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,transport);await app.refresh();assert.equal(app.snapshot.companies[0].name,'<img src=x onerror=1>');assert.match(app.status.textContent,/authoritative state v4/)});
test('Management submits versioned bounded intent metadata and exposes reconciliation state',async()=>{const root=new El('root');let sent;const transport={snapshot:async()=>({version:7}),intent:async envelope=>(sent=envelope,{state:'Conflict',code:'version-conflict',correlationId:envelope.correlationId}),exportLogs(){}};const context={document,globalThis:null,crypto:{randomUUID:(()=>{let i=0;return()=>`uuid-${++i}`})()},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,transport);await app.refresh();await app.submit('bdvm.management.wallet-transfer.v1',{amount:10});assert.equal(sent.moduleId,'BDVM.Management');assert.equal(sent.expectedVersion,7);assert.ok(sent.idempotencyKey);assert.equal(sent.payload.amount,10)});
test('destructive action confirmation can stop intent before transport',async()=>{const root=new El('root');let calls=0;const context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>false};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1}),intent:async()=>{calls++},exportLogs(){}});await app.refresh();await app.submit('bdvm.management.company-dissolve.v1',{}, {confirmation:'Dissolve?'});assert.equal(calls,0)});
test('frontend does not use HTML injection primitives or calculate balances and ownership',()=>{const source=fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8');assert.doesNotMatch(source,/\.innerHTML\s*=|insertAdjacentHTML|\beval\s*\(/);assert.doesNotMatch(source,/balance\s*[+\-*/]=|owner\s*=/i)});
test('industry details remain visible as inert JSON text',async()=>{const root=new El('root'),transport={snapshot:async()=>({version:1,industry:[{contractId:'c1',wagonRequirement:{cargoId:'Logs'},assignedWagons:[{assetId:'w1'}],manifests:[{loadedQuantity:4}]}]}),intent:async()=>({}),exportLogs(){}};const context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,transport);await app.refresh();const tabs=root.children[1];tabs.children.find(x=>x.textContent==='Industry').click();const visible=allText(root.children[root.children.length-1]);assert.ok(visible.some(value=>value.includes('cargoId')));assert.ok(visible.some(value=>value.includes('loadedQuantity'))) });
test('all management surfaces have English navigation labels',async()=>{const root=new El('root'),context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1}),intent:async()=>({}),exportLogs(){}});await app.refresh();const labels=root.children[1].children.map(x=>x.textContent);for(const expected of ['Companies','Wallets','Fleet','Catalog','Deliveries','Leases','Contracts','Industry','Passengers','Maintenance','Financing','Diagnostics'])assert.ok(labels.includes(expected))});
test('Catalog and Deliveries use distinct actionable empty states',async()=>{const root=new El('root'),context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1,market:[],deliveries:[]}),intent:async()=>({}),exportLogs(){}});await app.refresh();const tabs=root.children[1],panel=root.children[root.children.length-1];tabs.children[3].click();assert.match(panel.children[2].children[0].textContent,/No market offer exists yet/);tabs.children[4].click();assert.match(panel.children[2].children[0].textContent,/No rolling-stock delivery is pending/)});
test('complex surfaces explain their workflow and tabular data has visible column headings',async()=>{const root=new El('root'),context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1,diagnostics:[{subsystem:'World population',runtimeState:'Active'}]}),intent:async()=>({}),exportLogs(){}});await app.refresh();const tabs=root.children[1],panel=root.children[root.children.length-1];tabs.children.find(x=>x.textContent==='Industry').click();assert.match(panel.children[1].textContent,/Industry workflow/);tabs.children.find(x=>x.textContent==='Diagnostics').click();assert.match(panel.children[1].textContent,/Read-only health information/);const visible=allText(panel);assert.ok(visible.includes('Subsystem'));assert.ok(visible.includes('Runtime State'))});
test('browser-facing Management sources remain English-only',()=>{const sources=['app.js','app.css'].map(name=>fs.readFileSync(path.join(__dirname,'..','Assets',name),'utf8'));const french=/[àâçéèêëîïôùûüÿœ]|\b(?:actualiser|ouvrir|fermer|indisponible|itinéraire|trafic|joueur|compagnie|matériel|ferroviaire|déconnexion|connecté|hors ligne|rafraîchir|aucun|erreur|chargement|serveur|gare|voie|portefeuille|gestion|annuler|acheter|vendre|conducteur|affectation)\b/i;for(const source of sources)assert.doesNotMatch(source,french)});
test('schema-driven action forms submit typed values without browser-side business calculations',async()=>{const root=new El('root');let sent;const transport={snapshot:async()=>({version:9,actions:[{area:'leases',label:'Create lease',intentType:'bdvm.management.lease-manage.v1',payload:{action:'lease.manage',operation:'create-catalog-listings',listingIds:['l1']},fields:[{name:'deposit',label:'Deposit',kind:'number',value:'50',required:true},{name:'assetIds',label:'Assets',kind:'csv',value:'a,b'},{name:'forCompany',label:'Company',kind:'checkbox',value:'true'}]}]}),intent:async envelope=>(sent=envelope,{state:'Succeeded'}),exportLogs(){}};const context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,transport);await app.refresh();root.children[1].children.find(x=>x.textContent==='Leases').click();const panel=root.children[root.children.length-1],card=panel.children[4],button=card.children[card.children.length-1];button.click();await Promise.resolve();assert.equal(sent.payload.deposit,50);assert.deepEqual(Array.from(sent.payload.assetIds),['a','b']);assert.equal(sent.payload.forCompany,true);assert.equal(sent.payload.operation,'create-catalog-listings')});
test('host-provided compatible wagons are submitted from a multi-select without client filtering',async()=>{const root=new El('root');let sent;const transport={snapshot:async()=>({version:10,actions:[{area:'industry',label:'Assign compatible wagons',intentType:'bdvm.management.industry-manage.v1',payload:{action:'industry.manage',operation:'assign',contractId:'c1',forCompany:false},fields:[{name:'assetIds',label:'Compatible operator wagons',kind:'multiselect',required:true,options:['w1','w2']}]}]}),intent:async envelope=>(sent=envelope,{state:'Succeeded'}),exportLogs(){}};const context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,transport);await app.refresh();root.children[1].children.find(x=>x.textContent==='Industry').click();const panel=root.children[root.children.length-1],card=panel.children[4],select=card.children[1].children[1],button=card.children[card.children.length-1];select.selectedOptions=[{value:'w1'},{value:'w2'}];button.click();await Promise.resolve();assert.deepEqual(Array.from(sent.payload.assetIds),['w1','w2']);assert.equal(sent.payload.forCompany,false);assert.equal(sent.payload.contractId,'c1')});
test('selects display human labels while submitting authoritative IDs',async()=>{const root=new El('root'),context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1,actions:[{area:'passengers',label:'Create a passenger route',intentType:'bdvm.management.assignment-manage.v1',payload:{},fields:[{name:'originId',label:'Departure station',kind:'select',required:true,options:['SM'],optionLabels:{SM:'Steel Mill (ID: SM)'}}]}]}),intent:async()=>({state:'Succeeded'}),exportLogs(){}});await app.refresh();root.children[1].children.find(x=>x.textContent==='Passengers').click();const panel=root.children[root.children.length-1],visible=allText(panel);assert.ok(visible.includes('Steel Mill (ID: SM)'))});
test('host refusal exposes its actionable reason instead of pretending to be offline',async()=>{const root=new El('root'),context={document,globalThis:null,crypto:{randomUUID:()=> 'uuid'},confirm:()=>true};context.globalThis=context;vm.runInNewContext(fs.readFileSync(path.join(__dirname,'..','Assets','app.js'),'utf8'),context);const app=context.BdvmManagement.create(root,{snapshot:async()=>({version:1}),intent:async()=>{throw new Error('Selected model is already configured.')},exportLogs(){}});await app.refresh();await app.submit('bdvm.management.intent.v1',{});assert.match(app.status.textContent,/Selected model is already configured/);assert.doesNotMatch(app.status.textContent,/Offline/)});
